import 'vuetify/styles'
import { createVuetify } from 'vuetify'

import { VOverlay, VDialog, VSnackbar } from 'vuetify/components'

export default createVuetify({
  components: { VOverlay, VDialog, VSnackbar },
})
